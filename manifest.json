{
  "schemaVersion": 1,
  "id": "gaius-codius.unifi",
  "name": "UniFi",
  "version": "0.2.0",
  "author": "gaius-codius",
  "license": "MIT",
  "description": "Read-only UniFi Network site health on the Omarchy bar",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "UniFi",
    "description": "Site health, WAN status, and offline devices from UniFi Network",
    "category": "Network",
    "defaultSection": "right",
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 30,
      "compactMetric": "none",
      "dashboardUrl": ""
    },
    "schema": {
      "refreshIntervalSec": {
        "type": "integer",
        "default": 30,
        "minimum": 15,
        "maximum": 3600,
        "title": "Refresh interval (seconds)",
        "description": "How often the helper polls the controller."
      },
      "compactMetric": {
        "type": "string",
        "default": "none",
        "enum": [
          "none",
          "clients"
        ],
        "title": "Bar metric",
        "description": "Extra figure shown beside the icon. 'none' shows the icon alone."
      },
      "dashboardUrl": {
        "type": "string",
        "default": "",
        "title": "Dashboard URL",
        "description": "Opened by the panel's Open UniFi button. Empty derives it from the configured apiRoot host. http or https only."
      }
    }
  }
}
